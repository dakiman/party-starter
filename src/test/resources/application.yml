application:
  security:
    jwt-secret: "test-jwt-secret-must-be-at-least-32-characters-long-for-validation-pass"
    issuer: "PartyStarterTest"
    allowed-origins: "http://localhost:5173"
  spotify:
    auth:
      client-id: "test-spotify-client-id-not-real"
      client-secret: "test-spotify-secret"
      url: https://accounts.spotify.com
    url: https://api.spotify.com
  seeding:
    should-seed: false
  cocktail-db:
    url: https://www.thecocktaildb.com
    apikey: "1"

logging:
  level:
    root: warn
    com.example.partystarter: info
